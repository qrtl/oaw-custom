<?xml version="1.0" encoding="utf-8" ?>
<odoo>

    <record id="action_purchased_stock_data" model="ir.actions.act_window">
        <field name="name">Purchased Stock Data</field>
        <field name="type">ir.actions.act_window</field>
        <field name="res_model">supplier.stock</field>
        <field name="view_mode">tree</field>
        <field name="view_type">form</field>
        <field name="context">{'supplier_access_context': True}</field>
        <field name="domain">[('readonly_record', '=', True)]</field>
        <field name="view_id" ref="supplier_user_access.view_supplier_access_tree"/>
    </record>

    <record id="supplier_user_access.view_supplier_access_action" model="ir.actions.act_window">
        <field name="domain">[('readonly_record', '=', False)]</field>
    </record>

    <menuitem id="menu_action_purchased_stock_data" name="Purchased Stock Data"
              action="action_purchased_stock_data" parent="menu_supplier_stock_offer" sequence="2"/>

</odoo>
